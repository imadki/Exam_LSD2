double Rand()
{
  float nRand ;

  nRand= 2 * (float)rand() / (float)RAND_MAX - 1;

  return nRand;
}

double calculer_points_cercle(int INTERVALLE){

  /* Ecrire la fonction qui calcul le nombre de points */
}
  
int main(){

  /* tester la fonction en utilisant plusieurs valeur pour INTERVALLE */
}
